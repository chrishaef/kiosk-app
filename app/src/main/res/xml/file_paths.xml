<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Allow sharing files from the app's external files directory (where downloads are staged) -->
    <external-files-path name="my_downloads" path="Download" />
</paths>